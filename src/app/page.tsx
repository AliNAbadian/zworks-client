import { Button } from "@/components/ui/button";
import HeroSection from "@/components/ui/HeroSection";

const page = () => {
  return (
    <div>
      <HeroSection />
    </div>
  );
};

export default page;
