import { Button } from "@/components/ui/button";

const page = () => {
  return (
    <div>
      <Button
        // variant={"secondary"}
        className="cursor-pointer font-iransans "
      >
        سلام
      </Button>
    </div>
  );
};

export default page;
